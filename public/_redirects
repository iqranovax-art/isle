# Path redirects only.
# Cloudflare Pages does NOT support domain-level redirects here
# (http to https, www to apex). Those require:
# 1) functions/_middleware.js (deployed with this project)
# 2) Cloudflare SSL/TLS -> Always Use HTTPS
# 3) Bulk Redirect: www.marauderscheats.com -> https://marauderscheats.com
#    plus a proxied DNS A record for www -> 192.0.2.1

/sitemap-index.xml /sitemap.xml 301
/sitemap-0.xml /sitemap.xml 301
/sitemap-images.xml /sitemap.xml 301
