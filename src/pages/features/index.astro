---
import PageLayout from '../../layouts/PageLayout.astro';
import { productInfo } from '../../data/site';
import { maraudersImages } from '../../data/marauders';

const title = 'Marauders Cheats Features - ESP, Aimbot & More';
const description =
	'Browse Marauders Cheats features including ESP, Aimbot, interface toggles, package options, and update access.';
---

<PageLayout
	title={title}
	description={description}
	canonicalPath="/features/"
	heading="Marauders Cheats Features"
	intro="Compare ESP, Aimbot, and general package options in one place before choosing a duration on Pricing."
	heroImage={maraudersImages.hero}
	heroAlt="Marauders Cheats features overview visual"
	gallery={maraudersImages.gallery.slice(1, 4)}
	ctaPrimary={{ href: '/pricing/', label: 'Compare Pricing' }}
	ctaSecondary={{ href: '/faq/', label: 'Read FAQ' }}
>
	<section class="card-panel">
		<h2>ESP features</h2>
		<ul>
			{productInfo.features.esp.map((item) => <li>{item}</li>)}
		</ul>
		<div class="cta-row">
			<a class="cta-btn cta-btn--ghost" href="/marauders-esp/">ESP details</a>
		</div>
	</section>

	<section class="card-panel">
		<h2>Aimbot features</h2>
		<ul>
			{productInfo.features.aimbot.map((item) => <li>{item}</li>)}
		</ul>
		<div class="cta-row">
			<a class="cta-btn cta-btn--ghost" href="/marauders-aimbot/">Aimbot details</a>
		</div>
	</section>

	<section class="card-panel">
		<h2>General package features</h2>
		<ul>
			{productInfo.features.general.map((item) => <li>{item}</li>)}
		</ul>
	</section>
</PageLayout>
