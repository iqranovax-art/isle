{
  "name": "islecheats",
  "type": "module",
  "version": "0.0.4",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host --port 3000",
    "build": "astro build",
    "deploy": "node tools/wrangler/run.mjs pages deploy dist --project-name=isle --commit-dirty=true",
    "postinstall": "node scripts/link-wrangler.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/outfit": "^5.2.8",
    "@fontsource-variable/syne": "^5.2.7",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.9",
    "tailwindcss": "^4.3.2",
    "wrangler": "file:./tools/wrangler"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
